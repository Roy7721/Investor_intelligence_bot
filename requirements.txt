# Investor Intelligence — pinned to the versions the app is verified against.
#
#   python -m venv venv
#   venv\Scripts\pip install -r requirements.txt
#   uvicorn app.main:app --reload
#
# Derived from what the code actually imports, not from history. Anything not
# imported by app/, Rag/, ingestion/, vector_store/ or config/ is not here.

# --- web layer ---------------------------------------------------------
fastapi==0.141.1                 # API + static serving
uvicorn==0.52.0                  # ASGI server (not imported; runs the app)
python-multipart==0.0.32         # required by FastAPI to parse file uploads

# --- ingestion ---------------------------------------------------------
requests==2.34.2                 # Datalab (Marker) PDF -> markdown API
langchain-text-splitters==1.1.2  # RecursiveCharacterTextSplitter for prose

# --- models ------------------------------------------------------------
langchain-google-genai==4.3.2    # Gemini — answers and KPI extraction
openai==2.52.0                   # raw client, used only for OpenRouter embeddings

# --- storage -----------------------------------------------------------
chromadb==1.5.9                  # persistent vector store

# --- config ------------------------------------------------------------
python-dotenv==1.2.2             # loads API keys from .env

# Removed, and why — so they don't creep back:
#   langchain-openai   pulled in torch + transformers and cost 58s of import
#                      time for a Groq path that is no longer used. Removing it
#                      took app startup from 231s to 20.5s.
#   pymupdf4llm        the original PDF converter, replaced by Datalab after a
#                      four-tool comparison (see journey).
#   docling, torch,    installed during the extractor trial, rejected. Never
#   transformers       imported by the app; do not reinstall.
#   pydantic           transitive via fastapi, not pinned separately.
